<% @cart.line_items.each do |item| %>
	<%= item.quantity %>  --  <%= item.product.name %> -- 
	<%= link_to 'Удалить', method: :delete, data: { confirm: 'Are you sure?' } %>
	<br/>
<% end %>

<%= button_to 'Очистить корзину', @cart, method: :delete, data: { confirm: 'Удалить все товары из корзины ?' } %>
<%= button_to 'Оформить заказ', new_order_path, method: :get  %>